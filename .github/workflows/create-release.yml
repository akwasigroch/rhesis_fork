name: Hello GitHub

on:
  workflow_dispatch:  # Allows manual triggering

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "test create release workflow"
